{
  "name": "sinueux",
  "version": "0.0.0",
  "description": "TSX sibling of Sinuous with less twist",
  "keywords": [],
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://gitlab.com/nthm/sinueux"
  },
  "author": "Gen Hames",
  "scripts": {
    "build": "npm run build:h && npm run build:s",
    "build:h": "esbuild --bundle src/h/index.ts --outfile=publish/h.js --format=esm --minify --sourcemap",
    "build:s": "esbuild --bundle src/s/index.ts --outfile=publish/s.js --format=esm --minify --sourcemap",
    "dts": "tsc"
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^4.3.0",
    "@typescript-eslint/parser": "^4.3.0",
    "esbuild": "^0.7.8",
    "eslint": "^7.10.0",
    "typescript": "^4.0.3"
  }
}
